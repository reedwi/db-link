import { SiteConfig } from "@/types"

export const siteConfig: SiteConfig = {
  name: "db Link",
  description:
    "Link HubSpot records to database records",
  url: "https://db.com",
  links: {
    airi: "https://airi-labs.com/"
  }
}